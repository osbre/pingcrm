#!/usr/bin/env bash

MIX_ENV=cypress mix ecto.reset # Need a blank database
MIX_ENV=cypress mix phx.server &
pid=$! # Store server pid
./assets/node_modules/.bin/cypress open
result=$?
kill -9 $pid # Kill server
exit $result # Return test result
