#!/usr/bin/env bash

MIX_ENV=cypress mix ecto.reset
MIX_ENV=cypress mix run priv/repo/seeds.exs

MIX_ENV=cypress mix phx.server &
pid=$! # Store server pid
./assets/node_modules/.bin/cypress run
result=$?
kill -9 $pid # kill server
exit $result
